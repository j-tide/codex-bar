# README screenshots

The overview images are captured from the project’s native `MenuBarView`, using isolated sample data and disabled live refresh. They show the app UI, not an image-generated mockup.

| Language | Light | Dark |
| --- | --- | --- |
| 简体中文 | `overview-zh-light.png` | `overview-zh-dark.png` |
| English | `overview-en-light.png` | `overview-en-dark.png` |

All account identities, task titles, quotas, subscription dates, token totals, and model scores are demonstration data. They are not live account information or benchmark results. The capture version label identifies the documentation render, not a published release.

The READMEs select the matching hero appearance with `<picture>` and display all four variants in a linked screenshot gallery. When updating these assets, capture the actual native window compositor so Liquid Glass and AppKit controls render correctly. Use sample data, check both languages, and keep credentials and private task content out of the images.

The README logo comes directly from `codexBar/Assets.xcassets/AppIcon.appiconset/icon_256.png`.
